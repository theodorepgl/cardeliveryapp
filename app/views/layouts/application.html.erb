<!DOCTYPE html>
<html>
  <head>
    <title>CarDeliveryApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="application.scss">

    <style>
      body {
        background-color: #d6ccc2; /* Set body color to #d6ccc2 (light gray) */
      }

      .navbar {
        background-color: #edede9; /* Set navbar color to #edede9 (light gray) */
        box-shadow: 0px 4px;
      }

      .navbar-title {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        font-size: 40px;
        color: #333; /* Set navbar title text color to a dark gray */
      }

      .footer {
        background-color: #edede9; /* Set footer color to #edede9 (light gray) */
        color: #333; /* Set footer text color to a dark gray */
        text-align: center;
        padding: 10px;
        position: fixed;
        bottom: 0;
        width: 100%;
      }
    </style>
  </head>

  <body>
    <nav class="navbar p-4">
      <span class="navbar-title">Car Delivery</span>
      <%= link_to image_tag('download.jpeg', height: 100), root_path %>
      <% if user_signed_in? %>
        <div class="dropdown">
          <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </button>
          <ul class="dropdown-menu">
            <li><%= link_to 'Your Bookings', bookings_path %></li>
            <li><%= link_to 'Logout', destroy_user_session_path, data: {turbo_method: :delete} %></li>
          </ul>
        </div>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path %>
      <% end %>
    </nav>    
    <%= yield %>
    <div class="footer">
      Footer Content
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  </body>
</html>
